# k8s/flask-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ar-astro-svc
  namespace: audioreaper-astro
spec:
  type: ClusterIP
  selector:
    app: ar-astro
  ports:
    - port: 80
      targetPort: 8080
